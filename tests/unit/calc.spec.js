import {calculaPermutacao, calculaArranjo, calculaCombinatoria} from "@/utils/calc.js"

test('calc.js', calculaPermutacao => {

  expect(1).toBe(0);
  expect(1).toBe(1);
  expect(0).toBeNull();
  expect(1).toBeUndefined(0);
  expect(1).toBe(0);
  expect(1).toBe(0);
})
